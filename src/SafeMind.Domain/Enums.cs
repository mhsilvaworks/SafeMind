namespace SafeMind.Domain
{
    public enum TipoConta { Neurodivergente, Profissional, Empresa, Administrador }
    public enum StatusLaudo { Pendente, Verificado, Rejeitado }
    public enum TipoForum { B2CPublico, B2BTerapeutico }
    public enum TipoDocumento { LaudoMedico, CRM, CRP, CNPJ }
}